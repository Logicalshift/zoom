{
    IBClasses = (
        {
            ACTIONS = {
                deleteSavegame = id; 
                displayGameInfoWindow = id; 
                revealInFinder = id; 
                saveMetadata = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }, 
        {CLASS = NSSplitView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = ZoomCollapsableView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = ZoomCollapsingSplitView; LANGUAGE = ObjC; SUPERCLASS = NSSplitView; }, 
        {CLASS = ZoomFadeView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = ZoomFlipView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {CLASS = ZoomHQImageView; LANGUAGE = ObjC; SUPERCLASS = NSImageView; }, 
        {CLASS = ZoomSavePreviewView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            ACTIONS = {editSelectedCell = id; }; 
            CLASS = ZoomStoryTableView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSTableView; 
        }, 
        {CLASS = ZoomWhiteView; LANGUAGE = ObjC; SUPERCLASS = NSView; }, 
        {
            CLASS = ZoomiFButton; 
            LANGUAGE = ObjC; 
            OUTLETS = {pushedImage = NSImage; }; 
            SUPERCLASS = NSImageView; 
        }, 
        {
            ACTIONS = {
                addButtonPressed = id; 
                changeFilter1 = id; 
                changeFilter2 = id; 
                deleteSavegame = id; 
                flipToFilter = id; 
                flipToInfo = id; 
                flipToSaves = id; 
                goBack = id; 
                goForward = id; 
                goHome = id; 
                playIfdbGame = id; 
                restoreAutosave = id; 
                searchFieldChanged = id; 
                showIfDb = id; 
                showLocalGames = id; 
                startNewGame = id; 
            }; 
            CLASS = ZoomiFictionController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                addButton = ZoomiFButton; 
                backButton = NSButton; 
                browserView = NSView; 
                continueButton = ZoomiFButton; 
                currentUrl = NSTextField; 
                filterTable1 = NSTableView; 
                filterTable2 = NSTableView; 
                filterView = NSView; 
                flipButtonMatrix = NSMatrix; 
                flipView = ZoomFlipView; 
                forwardButton = NSButton; 
                gameDetailView = NSTextView; 
                gameImageView = NSImageView; 
                homeButton = NSButton; 
                ifdbView = WebView; 
                infoButton = ZoomiFButton; 
                infoView = NSView; 
                mainTableView = ZoomStoryTableView; 
                mainView = NSView; 
                newgameButton = ZoomiFButton; 
                picturePreview = NSWindow; 
                picturePreviewView = NSImageView; 
                playButton = NSButton; 
                previewView = ZoomSavePreviewView; 
                progressIndicator = NSProgressIndicator; 
                saveGameView = NSView; 
                saveMenu = NSMenu; 
                searchField = NSTextField; 
                splitView = ZoomCollapsingSplitView; 
                storyMenu = NSMenu; 
                topPanelView = NSView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}