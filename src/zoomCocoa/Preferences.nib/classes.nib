{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                autosaveChanged = id; 
                changeOrganiseDir = id; 
                changeTransparency = id; 
                displayWarningsChanged = id; 
                fatalWarningsChanged = id; 
                interpreterChanged = id; 
                keepOrganisedChanged = id; 
                reorganiseGames = id; 
                resetOrganiseDir = id; 
                revisionChanged = id; 
                speakGameTextChanged = id; 
            }; 
            CLASS = ZoomPreferenceWindow; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                autosaveGames = NSButton; 
                colourSettingsView = NSView; 
                colours = NSTableView; 
                displayWarnings = NSButton; 
                fatalWarnings = NSButton; 
                fontSettingsView = NSView; 
                fonts = NSTableView; 
                gameSettingsView = NSView; 
                generalSettingsView = NSView; 
                interpreter = NSPopUpButton; 
                keepGamesOrganised = NSButton; 
                organiseDir = NSTextView; 
                organiserIndicator = NSProgressIndicator; 
                reorganiseGames = NSButton; 
                revision = NSTextField; 
                speakGameText = NSButton; 
                transparencySlider = NSSlider; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}